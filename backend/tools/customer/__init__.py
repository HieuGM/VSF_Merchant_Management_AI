"""Customer tools package.

Tool modules (merchant_tools, customer_context_tools, weather_tools) each expose a
`register(registry)` entry point discovered by `registry.auto_discover("tools.customer")`;
no package-level import is needed here.
"""
