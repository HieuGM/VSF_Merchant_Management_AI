"- Mục tiêu Tuần 1: Triển khai crawl + chuẩn bị dữ liệu, xây dựng PRD, UseCase hoàn chỉnh cho dự án, setup repo, thiết kế database, kiến trúc tổng thể, luồng dữ liệu, hệ thống, xây dựng pipeline xử lý review, hình ảnh,
- Kết quả công việc Tuần 1: Triển khai crawl + chuẩn bị dữ liệu, xây dựng PRD, UseCase hoàn chỉnh cho dự án, setup repo, thiết kế database, kiến trúc tổng thể, luồng dữ liệu, hệ thống, xây dựng pipeline xử lý review, hình ảnh. Xây dựng luồng agent demo cho Customer
- Kế hoạch Tuần 2: Hoàn thiện Customer Agent
- Support needed:"	"- Mục tiêu Tuần 2: Triển khai xây dựng các api hoàn thiện, dựng tool, FE, memory, tối ưu thời gian cho customer agent 
- Kết quả công việc Tuần 2: Xây dựng thành công luồng demo Customer Agent
- Kế hoạch Tuần 3: Hoàn thiện, kiểm thử, đánh giá Customer Agent
- Support needed:"	"- Mục tiêu Tuần 3: Hoàn thiện, kiểm thử, đánh giá Customer Agent
- Kết quả công việc Tuần 3: Hoàn thành Plan A ""Unify Preference/Memory Store"" (6/6 phase), biến lời hứa ""ghi nhớ sở thích người dùng"" thành hiện thực — 1 store chuẩn mực (user_profiles), ranking tất định theo profile, context_memory dài hạn hoạt động thực sự, FE chuyển từ localStorage sang backend API. Thiết lập mô hình memory 3 lớp: structured profile / context_memory (dài hạn, cross-session) / prior_context (ngắn hạn, in-session anaphora). Cụ thể: (1) Backend profile REST GET/PATCH /api/v1/users/{id}/profile + IDOR guard require_dev_only; (2) Ranking tất định — profile_score cộng dồn ±0.15 vào match_score, hard-filter disliked cuisines, kill-switch config, mặc định an toàn không phá pipeline scoring hiện tại; (3) context_memory — auto-trích xuất fact dài hạn (dị ứng/chế độ ăn/vấn đề sức khỏe) bằng regex tiếng Việt, lưu JSONB, PII redact, FIFO cap 8; (4) FE cutover — Preference Center lưu thẳng backend, migrate 1 lần localStorage→API, đã verify live round-trip (toggle → user_profiles persist). Kiểm thử: pytest 127/0 (tăng dần 107→119→127, 0 fail), GT-eval giữ PARITY 39/39 qua từng phase (0 err/0 interrupt), code review từng phase APPROVE-with-nits và fix hết nit. Đánh giá: giữ vững baseline đáng tin 87.2% (34/39) → 39/39 sau khi sửa anaphora + mandatory-clarify (kế thừa cuối tuần 2). Docs sync: system-architecture (3-layer memory), roadmap, changelog, testing-guide, README.
- Kế hoạch Tuần 4: Hardening Customer Agent trước khi tích hợp — thay IDOR guard tạm bằng auth thật, A/B test trọng số ranking vs baseline, thêm UI edit context_memory, chạy lại LLM quality judge độc lập để làm tươi con số 87.2% rolled-forward. Bắt đầu hướng tới Phase 3 (Integration): chuẩn bị hợp nhất Customer + Merchant vertical, CI, kiểm thử hiệu năng, deployment-ready.
- Support needed: Hướng dẫn tích hợp auth thật cho PATCH profile (hiện placeholder require_dev_only, TODO(AUTH) trước khi expose prod)."