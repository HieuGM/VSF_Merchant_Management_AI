"Đã làm: Hardening dài hạn cho Customer Agent: (1) Bộ nhớ phân tầng — P1 mở rộng cửa sổ recall trong hội thoại (4→16 lượt), P2 distillate cross-conversation + purge tự động theo retention; (2) Củng cố memory dài hạn theo kết quả eval memory_test.json (28 case): khái quát hóa cảnh báo dị ứng theo world-knowledge, rút gọn note lỗi thời khi người dùng thu hồi, TTL cho ràng buộc tạm thời (""tuần này""→7 ngày), trường allergens riêng không bị FIFO đá ra; sửa hết 4 FAIL thực tế; (3) Lịch sử hội thoại kiểu ChatGPT: list/tìm kiếm/reopen/đổi tên/xóa session + khôi phục khi reload, kèm Playwright E2E pass; (4) Xây bộ metrics GT (Accuracy/Precision/Recall/F1) + chạy thử nghiệm LLM coordinator (độ trễ OK, chất lượng trung tính → đã revert, giữ kiến trúc coordinator-free); (5) Run-ready: chuyển giao máy mới 100% qua git (dump-restore + script portable). Kiểm thử: unit 243/243 pass (tăng 127→243), memory tests 16→30, GT-eval giữ PARITY 39/39, không phát sinh lỗi.
Kế tiếp: Bước vào Phase 3 (Integration): tích hợp Merchant Agent, thay IDOR guard tạm bằng auth thật, A/B test trọng số ranking vs baseline, thêm UI edit context_memory, CI + kiểm thử hiệu năng, hướng tới deployment-ready.
Vướng mắc: Cần hướng dẫn phương án auth thật cho các API PATCH/DELETE (hiện placeholder require_dev_only, TODO(AUTH) trước khi expose prod)."