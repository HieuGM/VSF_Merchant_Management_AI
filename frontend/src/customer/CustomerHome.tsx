/**
 * Customer Discovery entry (Dev A owns everything under src/customer/).
 * Phase 0 placeholder — build search, map, chat, preference center here (Feature G/K).
 */
export function CustomerHome() {
  return (
    <section className="p-6">
      <h2 className="mb-2 text-xl font-semibold">Khám phá quán ăn</h2>
      <p className="text-sm text-gray-500">
        Phase 0 skeleton. Dev A: xây tìm kiếm, bản đồ, trợ lý khách hàng ở đây.
      </p>
    </section>
  );
}
