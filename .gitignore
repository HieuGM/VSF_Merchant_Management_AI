# Raw crawl + build caches (heavy/regenerable — kept OUT of git).
# NOTE: the dataset deliverables (profiles.jsonl, merchants_unique.jsonl,
# shopeefood_catalog.jsonl) + merchant_platform_full.dump ARE committed
# so a fresh machine can import data without OneDrive/USB.
data/crawled/
data/profile_cache/
data/synthetic/
plans/reports/crawl-progress.log
plans/reports/*-batch.log
plans/reports/model-compare.log

# Claude Code / claude-kit tooling (không đưa lên repo dự án)
.claude/
.agents/
CLAUDE.md
AGENTS.md
.mcp.json
.opencode/
.playwright-mcp/

# Superpowers tooling
.superpowers
docs/superpowers/*

# Python
__pycache__/
*/__pycache__
*.pyc
.venv/
.venv/*
.pytest_cache

# Coverage artifacts (regenerable test byproducts)
.coverage
coverage.json
htmlcov/

# Logs
*.log

# Env / secrets
.env
.env.*
!.env.example
*.key

# DB dumps: stale .sql kept out; the active transfer dump IS committed.
merchant_platform*.sql

# OS / editor
.DS_Store
Thumbs.db

test.py